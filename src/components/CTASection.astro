---
import { Heart, Phone, Mail, ArrowRight, Sparkles } from 'lucide-react';
---

<section
  class="py-32 bg-gradient-to-br from-blue-600 via-purple-600 to-pink-600 relative overflow-hidden"
>
  <!-- Animated Background Elements -->
  <div class="absolute inset-0 overflow-hidden">
    <div
      class="absolute top-0 left-0 w-full h-full bg-[radial-gradient(circle_at_20%_80%,rgba(255,255,255,0.1)_0%,transparent_50%)]"
    >
    </div>
    <div
      class="absolute top-0 right-0 w-full h-full bg-[radial-gradient(circle_at_80%_20%,rgba(255,255,255,0.1)_0%,transparent_50%)]"
    >
    </div>
    <div class="absolute inset-0 opacity-10">
      <div
        class="absolute inset-0"
        style="background-image: radial-gradient(circle at 1px 1px, rgba(255,255,255,0.3) 1px, transparent 0); background-size: 20px 20px;"
      >
      </div>
    </div>
  </div>

  <!-- Floating Elements -->
  <div class="absolute top-20 left-20 animate-float">
    <div
      class="w-16 h-16 bg-white/10 backdrop-blur-lg rounded-2xl flex items-center justify-center border border-white/20"
    >
      <Heart className="w-8 h-8 text-white animate-pulse" />
    </div>
  </div>

  <div class="absolute bottom-32 right-32 animate-float-delay">
    <div
      class="w-20 h-20 bg-white/10 backdrop-blur-lg rounded-2xl flex items-center justify-center border border-white/20"
    >
      <Sparkles className="w-10 h-10 text-white animate-pulse" />
    </div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Main Content -->
    <div class="max-w-4xl mx-auto">
      <!-- Badge -->
      <div
        class="inline-flex items-center px-6 py-3 bg-white/10 backdrop-blur-lg rounded-full border border-white/20 mb-8"
      >
        <Sparkles className="w-5 h-5 text-yellow-300 mr-2 animate-pulse" />
        <span class="text-white font-semibold">Únete a nuestra comunidad</span>
      </div>

      <!-- Title -->
      <h2 class="text-4xl md:text-6xl font-bold text-white mb-6 leading-tight">
        <span class="block">¿Listo para hacer la</span>
        <span class="block text-yellow-300">diferencia?</span>
      </h2>

      <!-- Description -->
      <p class="text-xl text-white/90 mb-12 max-w-2xl mx-auto leading-relaxed">
        Cada persona que se une a nuestra asociación nos ayuda a construir una sociedad más
        inclusiva y accesible para todos.
      </p>

      <!-- CTA Buttons -->
      <div class="flex flex-col lg:flex-row gap-6 justify-center items-center mb-16">
        <a
          href="/inscripcion/"
          class="group inline-flex items-center px-10 py-5 bg-white text-blue-600 font-bold text-lg rounded-3xl hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 shadow-2xl hover:shadow-white/25"
        >
          <Heart className="w-6 h-6 mr-3 group-hover:animate-bounce" />
          Quiero asociarme
          <ArrowRight className="w-6 h-6 ml-3 group-hover:translate-x-1 transition-transform" />
        </a>

        <a
          href="/contacto/"
          class="group inline-flex items-center px-10 py-5 bg-white/10 backdrop-blur-lg text-white font-bold text-lg rounded-3xl hover:bg-white/20 border-2 border-white/30 hover:border-white/50 transition-all duration-300 transform hover:scale-105"
        >
          <Phone className="w-6 h-6 mr-3" />
          Contactar
          <ArrowRight className="w-6 h-6 ml-3 group-hover:translate-x-1 transition-transform" />
        </a>
      </div>

      <!-- Contact Info -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-3xl mx-auto">
        <div class="group text-center">
          <div
            class="w-16 h-16 bg-white/10 backdrop-blur-lg rounded-2xl flex items-center justify-center mx-auto mb-4 border border-white/20 group-hover:scale-110 group-hover:bg-white/20 transition-all duration-300"
          >
            <Phone className="w-8 h-8 text-white" />
          </div>
          <div class="text-white font-semibold">600 000 000</div>
          <div class="text-white/70 text-sm">WhatsApp</div>
        </div>

        <div class="group text-center">
          <div
            class="w-16 h-16 bg-white/10 backdrop-blur-lg rounded-2xl flex items-center justify-center mx-auto mb-4 border border-white/20 group-hover:scale-110 group-hover:bg-white/20 transition-all duration-300"
          >
            <Mail className="w-8 h-8 text-white" />
          </div>
          <div class="text-white font-semibold">info@divermataro.org</div>
          <div class="text-white/70 text-sm">Email</div>
        </div>

        <div class="group text-center">
          <div
            class="w-16 h-16 bg-white/10 backdrop-blur-lg rounded-2xl flex items-center justify-center mx-auto mb-4 border border-white/20 group-hover:scale-110 group-hover:bg-white/20 transition-all duration-300"
          >
            <Heart className="w-8 h-8 text-white" />
          </div>
          <div class="text-white font-semibold">Mataró</div>
          <div class="text-white/70 text-sm">Barcelona</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bottom Wave -->
  <div class="absolute bottom-0 left-0 w-full overflow-hidden">
    <svg
      class="relative block w-full h-16"
      xmlns="http://www.w3.org/2000/svg"
      viewBox="0 0 1200 120"
      preserveAspectRatio="none"
    >
      <path
        d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0,0,0,321.39,56.44Z"
        class="fill-white dark:fill-gray-900"></path>
    </svg>
  </div>
</section>

<style>
  @keyframes float {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes float-delay {
    0%,
    100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-15px);
    }
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-float-delay {
    animation: float-delay 8s ease-in-out infinite;
  }
</style>
