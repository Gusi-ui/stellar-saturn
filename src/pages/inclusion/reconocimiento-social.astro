---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Award, Users, School, Building, Presentation, Star, Phone, Mail } from 'lucide-react';

const programas = [
  {
    icon: School,
    title: 'Charlas Educativas',
    description:
      'Programas de sensibilización en escuelas, institutos y universidades sobre diversidad funcional.',
    color: 'from-yellow-500 to-orange-500',
  },
  {
    icon: Building,
    title: 'Colaboraciones Empresariales',
    description:
      'Trabajamos con empresas para promover la inclusión laboral y el reconocimiento del talento diverso.',
    color: 'from-orange-500 to-amber-500',
  },
  {
    icon: Presentation,
    title: 'Eventos de Visibilización',
    description:
      'Organizamos eventos públicos para mostrar las capacidades y logros de personas con diversidad funcional.',
    color: 'from-amber-500 to-yellow-500',
  },
];

const impacto = [
  'Más de 50 charlas realizadas en centros educativos',
  '20+ empresas colaboradoras en programas de inclusión',
  'Reconocimiento público de talentos diversos',
  'Cambio de percepción social sobre la diversidad funcional',
  'Creación de referentes positivos en medios de comunicación',
  'Premios y reconocimientos a personas con diversidad funcional',
];
---

<BaseLayout
  title="Reconocimiento Social | DiverMataró"
  description="Promovemos el valor y las capacidades de las personas con diversidad funcional a través de programas de sensibilización y visibilización."
  image="/logoDiverMataro.png"
>
  <!-- Hero Section -->
  <section
    class="relative pt-32 pb-20 bg-gradient-to-br from-yellow-50 to-orange-50 dark:from-gray-900 dark:to-yellow-900/20 overflow-hidden"
  >
    <!-- Background Pattern -->
    <div class="absolute inset-0 opacity-5">
      <div
        class="absolute inset-0"
        style="background-image: radial-gradient(circle at 1px 1px, rgba(0,0,0,0.15) 1px, transparent 0); background-size: 20px 20px;"
      >
      </div>
    </div>

    <!-- Decorative Elements -->
    <div
      class="absolute top-20 right-20 w-72 h-72 bg-gradient-to-br from-yellow-400/20 to-orange-400/20 rounded-full blur-3xl"
    >
    </div>
    <div
      class="absolute bottom-20 left-20 w-96 h-96 bg-gradient-to-tr from-amber-400/20 to-yellow-400/20 rounded-full blur-3xl"
    >
    </div>

    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Breadcrumb -->
      <nav class="mb-8" aria-label="Breadcrumb">
        <ol class="flex items-center space-x-2 text-sm">
          <li>
            <a
              href="/"
              class="text-gray-600 dark:text-gray-400 hover:text-yellow-600 dark:hover:text-yellow-400 transition-colors"
              >Inicio</a
            >
          </li>
          <li class="text-gray-400">/</li>
          <li class="text-yellow-600 dark:text-yellow-400 font-semibold">Reconocimiento Social</li>
        </ol>
      </nav>

      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <div>
          <!-- Icon Badge -->
          <div
            class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-yellow-100 to-orange-100 dark:from-yellow-900/30 dark:to-orange-900/30 rounded-full mb-6"
          >
            <Award className="w-5 h-5 text-yellow-600 mr-2" />
            <span class="text-sm font-semibold text-yellow-700 dark:text-yellow-300"
              >Reconocimiento Social</span
            >
          </div>

          <h1 class="text-5xl md:text-6xl font-bold text-gray-900 dark:text-white mb-6">
            Visibilizando capacidades
          </h1>

          <p class="text-xl text-gray-600 dark:text-gray-300 mb-8">
            Trabajamos para cambiar la percepción social sobre la diversidad funcional, promoviendo
            el reconocimiento del valor, las capacidades y los logros de las personas.
          </p>

          <div class="flex flex-col sm:flex-row gap-4">
            <a
              href="/contacto/"
              class="inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-yellow-600 to-orange-600 text-white font-semibold rounded-2xl hover:from-yellow-700 hover:to-orange-700 transition-all duration-300 transform hover:scale-105 shadow-lg"
            >
              Colabora con nosotros
              <Mail className="w-5 h-5 ml-2" />
            </a>
            <a
              href="#programas"
              class="inline-flex items-center justify-center px-8 py-4 bg-white dark:bg-gray-800 text-yellow-600 dark:text-yellow-400 font-semibold rounded-2xl hover:bg-gray-50 dark:hover:bg-gray-700 transition-all duration-300 border-2 border-yellow-200 dark:border-yellow-800"
            >
              Ver programas
            </a>
          </div>
        </div>

        <!-- Image Placeholder -->
        <div class="relative">
          <div
            class="aspect-square bg-gradient-to-br from-yellow-400 to-orange-500 rounded-3xl shadow-2xl flex items-center justify-center"
          >
            <Award className="w-32 h-32 text-white/80" />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Programas Section -->
  <section id="programas" class="py-32 bg-white dark:bg-gray-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6">
          Nuestros Programas
        </h2>
        <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
          Desarrollamos diferentes programas para promover el reconocimiento social y cambiar
          percepciones.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {
          programas.map((programa, index) => (
            <div
              class="group bg-white dark:bg-gray-800 rounded-3xl p-8 shadow-lg hover:shadow-2xl transition-all duration-500 hover:-translate-y-2 border border-gray-100 dark:border-gray-700"
              data-aos="fade-up"
              data-aos-delay={index * 100}
            >
              <div
                class={`w-16 h-16 bg-gradient-to-br ${programa.color} rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300`}
              >
                <programa.icon className="w-8 h-8 text-white" />
              </div>
              <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">
                {programa.title}
              </h3>
              <p class="text-gray-600 dark:text-gray-300">{programa.description}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Impacto Section -->
  <section
    class="py-32 bg-gradient-to-br from-gray-50 to-yellow-50 dark:from-gray-800 dark:to-yellow-900/10"
  >
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <div class="relative">
          <div
            class="aspect-square bg-gradient-to-br from-yellow-400 to-orange-500 rounded-3xl shadow-2xl flex items-center justify-center"
          >
            <Star className="w-32 h-32 text-white/80" />
          </div>
        </div>

        <div>
          <h2 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6">
            Nuestro impacto
          </h2>
          <p class="text-xl text-gray-600 dark:text-gray-300 mb-8">
            Los resultados de nuestro trabajo de sensibilización y visibilización hablan por sí
            mismos.
          </p>

          <div class="space-y-4">
            {
              impacto.map((item, index) => (
                <div
                  class="flex items-start gap-4"
                  data-aos="fade-left"
                  data-aos-delay={index * 50}
                >
                  <div class="flex-shrink-0 w-6 h-6 bg-gradient-to-br from-yellow-500 to-orange-500 rounded-full flex items-center justify-center mt-1">
                    <Star className="w-4 h-4 text-white" />
                  </div>
                  <p class="text-lg text-gray-700 dark:text-gray-300">{item}</p>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonios Section -->
  <section class="py-32 bg-white dark:bg-gray-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-white mb-6">
          Historias de éxito
        </h2>
        <p class="text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto">
          Personas con diversidad funcional que han conseguido el reconocimiento que merecen.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {
          [
            {
              name: 'María García',
              role: 'Artista reconocida',
              quote:
                'Gracias a DiverMataró pude mostrar mi arte al mundo y ser valorada por mi talento.',
            },
            {
              name: 'Carlos Ruiz',
              role: 'Emprendedor',
              quote:
                'El apoyo de la asociación fue clave para que mi empresa fuera reconocida en el sector.',
            },
            {
              name: 'Laura Martínez',
              role: 'Deportista',
              quote:
                'Conseguí visibilizar el deporte adaptado y ser un referente para otras personas.',
            },
          ].map((testimonio, index) => (
            <div
              class="bg-gradient-to-br from-yellow-50 to-orange-50 dark:from-gray-800 dark:to-yellow-900/10 rounded-3xl p-8 shadow-lg"
              data-aos="fade-up"
              data-aos-delay={index * 100}
            >
              <div class="flex items-center mb-4">
                <div class="w-12 h-12 bg-gradient-to-br from-yellow-500 to-orange-500 rounded-full flex items-center justify-center mr-4">
                  <Users className="w-6 h-6 text-white" />
                </div>
                <div>
                  <h3 class="font-bold text-gray-900 dark:text-white">{testimonio.name}</h3>
                  <p class="text-sm text-gray-600 dark:text-gray-400">{testimonio.role}</p>
                </div>
              </div>
              <p class="text-gray-700 dark:text-gray-300 italic">"{testimonio.quote}"</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-32 bg-gradient-to-r from-yellow-600 to-orange-600 relative overflow-hidden">
    <div class="absolute inset-0 opacity-10">
      <div
        class="absolute inset-0"
        style="background-image: radial-gradient(circle at 2px 2px, rgba(255,255,255,0.3) 1px, transparent 0); background-size: 40px 40px;"
      >
      </div>
    </div>

    <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        ¿Quieres colaborar en la visibilización?
      </h2>
      <p class="text-xl text-white/90 mb-8 max-w-2xl mx-auto">
        Únete a nosotros para promover el reconocimiento social de las personas con diversidad
        funcional.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/contacto/"
          class="inline-flex items-center justify-center px-8 py-4 bg-white text-yellow-600 font-semibold rounded-2xl hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 shadow-lg"
        >
          Contactar ahora
          <Phone className="w-5 h-5 ml-2" />
        </a>
        <a
          href="/"
          class="inline-flex items-center justify-center px-8 py-4 bg-transparent border-2 border-white text-white font-semibold rounded-2xl hover:bg-white hover:text-yellow-600 transition-all duration-300"
        >
          Volver al inicio
        </a>
      </div>
    </div>
  </section>
</BaseLayout>
